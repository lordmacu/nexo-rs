# Rust
target/
**/*.rs.bk
Cargo.lock

# Secrets — never commit
secrets/*
!secrets/.gitkeep
!secrets/*.example
.env
.env.*
!.env.example
*.key
*.pem
*.nkey

# Runtime data
data/
logs/
# Cache of resolved peer devices per JID. Contains contacts' phone
# numbers (PII) and only makes sense for a specific paired account.
device_cache.json
# Local dev artifacts
screenshot.png
*.patch

# Agent runtime logs
agent.log
*.log

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
