# Runtime deps for the Python plugin template.
#
# The plugin SDK ships on PyPI as `nexoai` (the `nexo-plugin-sdk` name
# was taken); the importable module is `nexo_plugin_sdk`. Source repo:
# https://github.com/lordmacu/nexo-plugin-sdks
#
# `pack-tarball-python.sh` vendors these into `lib/` via
# `pip install --target lib -r requirements.txt`.
nexoai==0.2.0
tomli; python_version < "3.11"
