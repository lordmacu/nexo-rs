[package]
name = "nexo-tunnel"
description   = "Legacy alias for `nexo-tunnel-quick` — re-exports every public item verbatim so existing callers keep building. Phase 92.11 retires this crate; prefer `nexo-tunnel-quick` in new code."
version       = "0.3.0"
edition       = { workspace = true }
rust-version  = { workspace = true }
license       = { workspace = true }
authors       = { workspace = true }
repository    = { workspace = true }
homepage      = { workspace = true }
documentation = { workspace = true }
readme        = "README.md"
keywords      = { workspace = true }
categories    = { workspace = true }

[dependencies]
nexo-tunnel-quick = { workspace = true }

[lints]
workspace = true
